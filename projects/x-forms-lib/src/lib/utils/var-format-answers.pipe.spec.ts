import { VarFormatAnswersPipe } from './var-format-answers.pipe';

describe('VarFormatAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new VarFormatAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
