import { VarFormatPipe } from './var-format.pipe';

describe('VarFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VarFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
