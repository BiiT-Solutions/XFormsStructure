import { NestedAnswersPipe } from './nested-answers.pipe';

describe('NestedAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new NestedAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
