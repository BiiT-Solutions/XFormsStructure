import { SliderConverterPipe } from './slider-converter.pipe';

describe('SliderConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new SliderConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
