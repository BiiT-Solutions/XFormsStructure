import { CheckAnswersPipe } from './check-answers.pipe';

describe('CheckAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
